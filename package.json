{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "scripts": {
    "start": "lerna exec --scope @zidol/next2 -- npm start"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "dependencies": {
    "next-transpile-modules": "^9.1.0"
  }
}
