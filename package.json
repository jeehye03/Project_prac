{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "scripts": {
    "start": "npx lerna run --scope next2 --stream dev"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "dependencies": {
    "jotai-tanstack-query": "^0.4.0",
    "next-transpile-modules": "^9.1.0"
  }
}
